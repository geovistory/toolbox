{
  "name": "geovistory",
  "scripts": {},
  "env": {
    "DB_ENV": "review",
    "HEROKU_APP_NAME": {
      "required": true
    },
    "GV_DB_SSL": "sslmode=require",
    "WH_DB_SSL": "sslmode=require",
    "WAREHOUSE_GV_DB_POOL_SIZE":"5",
    "WAREHOUSE_WH_DB_POOL_SIZE":"5",
    "WH_DATABASE_URL": "${DATABASE_URL}"
  },
  "formation": {},
  "addons": [
    {
      "plan": "heroku-postgresql:hobby-basic",
      "options": {
        "version": "10"
      }
    },
    {
      "plan": "heroku-postgresql:hobby-basic",
      "options": {
        "version": "10"
      }
    }
  ],
  "buildpacks": [
    {
      "url": "heroku/nodejs"
    }
  ]
}