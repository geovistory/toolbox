{
  "name": "geovistory",
  "scripts": {
    "postdeploy": "echo 'postinstall DB_ENV: ' && echo $DB_ENV && node_modules/db-migrate/bin/db-migrate --config server/migrate-db-config.json --migrations-dir server/migrations up"
  },
  "env": {
    "DB_ENV": "review"
  },
  "formation": {
  },
  "addons": [

  ],
  "buildpacks": [
    {
      "url": "heroku/nodejs"
    }
  ]
}