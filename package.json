{
  "name": "geovistory-root",
  "description": "package.json for DevOps scripts",
  "version": "0.2.3",
  "scripts": {
    "******************* development ***************": "",
    "dev:build": "mkdir -p pg_logs && docker-compose -f docker-compose.yaml --project-name geov_dev up --build -d",
    "dev:up": "mkdir -p pg_logs && docker-compose -f docker-compose.yaml --project-name geov_dev up -d",
    "dev:down": "docker-compose -f docker-compose.yaml --project-name geov_dev down"
  },
  "private": true,
  "main": "index.js",
  "cacheDirectories": [
    "server/node_modules",
    "client/node_modules"
  ]
}