import { PassiveLinkDirective } from './passive-link.directive';

describe('PassiveLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new PassiveLinkDirective();
    expect(directive).toBeTruthy();
  });
});
