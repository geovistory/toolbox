import { ViewFieldDropListDirective } from './view-field-drop-list.directive';

describe('ViewFieldDropListDirective', () => {
  it('should create an instance', () => {
    const directive = new ViewFieldDropListDirective();
    expect(directive).toBeTruthy();
  });
});
