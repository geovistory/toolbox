import { Notifications } from './notifications.models';
const INITIAL_STATE = new Notifications();
export function notificationsReducer(state = INITIAL_STATE, a) {
    const action = a;
    switch (action.type) {
    }
    return state;
}
;
//# sourceMappingURL=notifications.reducer.js.map