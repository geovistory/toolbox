import { BasicModule } from './basic.module';
describe('BasicModule', () => {
    let basicModule;
    beforeEach(() => {
        basicModule = new BasicModule();
    });
    it('should create an instance', () => {
        expect(basicModule).toBeTruthy();
    });
});
//# sourceMappingURL=basic.module.spec.js.map