import { DataModule } from './data.module';
describe('DataModule', () => {
    let dataModule;
    beforeEach(() => {
        dataModule = new DataModule();
    });
    it('should create an instance', () => {
        expect(dataModule).toBeTruthy();
    });
});
//# sourceMappingURL=data.module.spec.js.map