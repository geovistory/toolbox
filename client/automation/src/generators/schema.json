{
  "$schema": "http://json-schema.org/schema",
  "$id": "AngularComponent",
  "title": "",
  "type": "object",
  "properties": {
    "componentFolder": {
      "type": "string",
      "description": "Relative path of the new component folder (e.g. 'apps/app-toolbox/src/app/components/misc/new-component')",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What is the path of the new component folder (e.g. 'apps/app-toolbox/src/app/components/misc/new-component')?"
    }
  },
  "required": [
    "componentFolder"
  ]
}