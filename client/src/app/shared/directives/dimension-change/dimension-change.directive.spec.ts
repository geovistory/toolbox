import { DimensionChangeDirective } from './dimension-change.directive';

describe('DimensionChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new DimensionChangeDirective();
    expect(directive).toBeTruthy();
  });
});
