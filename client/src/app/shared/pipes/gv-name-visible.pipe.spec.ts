import { GvNameVisiblePipe } from './gv-name-visible.pipe';

describe('GvNameVisiblePipe', () => {
  it('create an instance', () => {
    const pipe = new GvNameVisiblePipe();
    expect(pipe).toBeTruthy();
  });
});
