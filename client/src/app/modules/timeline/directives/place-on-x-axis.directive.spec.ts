import { PlaceOnXAxisDirective } from './place-on-x-axis.directive';

describe('PlaceOnXAxisDirective', () => {
  it('should create an instance', () => {
    const directive = new PlaceOnXAxisDirective();
    expect(directive).toBeTruthy();
  });
});
