import { PanelBodyDirective } from './panel-body.directive';

describe('PanelBodyDirective', () => {
  it('should create an instance', () => {
    const directive = new PanelBodyDirective();
    expect(directive).toBeTruthy();
  });
});
