import { WrapTextDirective } from './wrap-text.directive';

describe('WrapTextDirective', () => {
  it('should create an instance', () => {
    const directive = new WrapTextDirective();
    expect(directive).toBeTruthy();
  });
});
